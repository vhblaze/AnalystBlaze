name: Security Pentest

on:
  push:
    branches: [main]

jobs:
  desktop-security:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: src-tauri
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Run adaptive optimizer regressions
        run: cargo test "optimizations::"
